node_modules/
dist/
release/
*.tsbuildinfo
.env
*.db
*.db-journal
# Claude Code / agent tooling state
.claude/worktrees/
.omc/
.serena/
coverage/
.review/
.omx/

# local pairing token for the diagnostic probe
.wm-probe-token

# developer's own cafe; the source ships none
config/local.json

# macOS drops these into any folder Finder has looked at
.DS_Store
