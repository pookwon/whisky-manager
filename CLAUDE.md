# whisky-manager

## 시각 표시

**언제나 KST로 보여준다.** 머신 시간대에도 UTC에도 의존하지 않는다. 카페와 운영자가 KST에 있고, 이 도구가 도는 기계가 어느 시간대에 맞춰져 있는지는 우연이다.

`getUTCHours()`를 그대로 읽거나 `toLocaleString()`에 시간대를 맡기지 않는다. 오프셋은 `KST_OFFSET_MS` (`src/shared/kst.ts`) 하나뿐이고, 하루의 경계도 KST 자정이며 그 계산은 `kstDayRange`가 갖는다.

어긋난 시각은 조용히 틀린다. "다음 세션 02:16"이 실제로는 11:16이면 운영자는 새벽까지 쉬는 것으로 읽고, 1분 뒤에 도는 것을 놓친다.

## 언어

**한국어 전용이다. 다국어 지원은 요구사항이 아니다.** 전역 규칙의 i18n 요구는 이 프로젝트에 적용하지 않는다.

문구는 `src/shared/text.ts`에 모아 둔다. 번역 대상이어서가 아니라, 사용자에게 보이는 말을 한자리에서 읽고 고치기 위해서다. 문구를 컴포넌트에 직접 박지 않는 이유도 그것뿐이다.

`renderer`가 아니라 `shared`에 있는 이유는 운영자에게 말을 거는 표면이 셋이기 때문이다 — 렌더러, 메인 프로세스의 트레이 메뉴, 그리고 파일 대화상자처럼 셸이 여는 것들.

키가 아니라 값을 그대로 두는 것도 의도다. `TEXT.run.confrim`은 빌드를 깨뜨리지만 `t('run.confrim')`은 그 문자열 그대로 화면에 나간다. 값을 받는 문구를 함수로 두는 이유도 같다 — 값을 빠뜨리면 컴파일이 안 되므로 `{{count}}`가 화면에 남을 수 없다.
